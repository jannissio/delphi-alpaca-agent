# secrets — never commit (no template file either: the variables are listed in README, Setup)
.env
.env.*
api_keys_NEVER_PUBLISH/
*.key
*.pem

# copyrighted papers and extracted text stay local
research/sources/
research/sources_txt/

# python
.venv/
venv/
__pycache__/
*.pyc
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
build/
dist/

# runtime state and logs (the audit log is published as docs/ledger.json and the dashboard, curated)
runtime/
logs/
*.log
state/

# os / editor
.DS_Store
Thumbs.db
.vscode/
.idea/

# historical downloads and simulation arrays (regenerate with scripts/history_data.py)
state/history/
state/*.npy
